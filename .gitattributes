# Auto detect text files and normalize line endings to LF in the repository
* text=auto

# Force PowerShell files to use LF line endings (matching repository)
*.ps1 text eol=lf
*.psm1 text eol=lf
*.psd1 text eol=lf